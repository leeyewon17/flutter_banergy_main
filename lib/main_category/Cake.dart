import 'package:flutter/material.dart';
import 'dart:io';

class CakeScreenn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('cake'),
      ),
      body: Center(
        child: Text('This is the cake Screen!'),
      ),
    );
  }
}
