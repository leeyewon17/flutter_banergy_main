import 'package:flutter/material.dart';
import 'dart:io';

class DrinkScreenn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Drink Screen'),
      ),
      body: Center(
        child: Text('This is the drink Screen!'),
      ),
    );
  }
}
