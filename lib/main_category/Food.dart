import 'package:flutter/material.dart';
import 'dart:io';

class FoodScreenn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Food Screen'),
      ),
      body: Center(
        child: Text('This is the food Screen!'),
      ),
    );
  }
}
