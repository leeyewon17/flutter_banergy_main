import 'package:flutter/material.dart';
import 'dart:io';

class FastfoodScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fast food Screen'),
      ),
      body: Center(
        child: Text('This is the fast food screen!'),
      ),
    );
  }
}
